-- ============================================================
-- 心之使者「小隊長」標記欄位
-- ============================================================
--
-- 背景：分組總表原本用「該組累積年資最長者」近似推斷小隊長身分（視覺上
-- 置頂），但小隊長實際上是任命制、與年資無關（可能異動、可能新人被指派
-- 但年資不是最長）。需要一個獨立欄位明確記錄，不再依賴年資推斷。
--
-- 語意：true=該組小隊長；false/null=一般組員。理論上每組應恰有一位
-- true，但這個約束不在資料庫層強制（見 openspec/changes/
-- spirit-ambassador-master-roster 的風險說明——由應用層/人工維護保證）。
ALTER TABLE students
  ADD COLUMN IF NOT EXISTS spirit_ambassador_is_leader BOOLEAN;
